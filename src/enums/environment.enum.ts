export enum Environments {
  PRODUCTION = 'production',
  DEV = 'development',
  TEST = 'test',
  STAGING = 'staging',
}

export enum EnvironmentFile {
  DEFAULT = '.env',
  PRODUCTION = '.env.prod',
  DEV = '.env.dev',
  TEST = '.env.test',
  STAGING = '.env.stage',
}
